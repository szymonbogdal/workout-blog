# Enable the Rewrite Engine
RewriteEngine On

# Set the base directory
RewriteBase /workout_blog/

# Allow direct access to public directory
RewriteRule ^public/ - [L]

# Redirect other requests to index.php
RewriteRule ^ index.php [L]